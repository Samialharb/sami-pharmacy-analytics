# دليل المساهمة

شكراً لاهتمامك بالمساهمة في منصة التقارير والإحصائيات لصيدلية سامي! 🎉

## قواعد المساهمة

### 1. قبل البدء

- تأكد من أن لديك حساب GitHub
- قم بـ fork المستودع
- استنسخ المستودع المفروع إلى جهازك المحلي

```bash
git clone https://github.com/YOUR_USERNAME/sami-pharmacy-analytics.git
cd sami-pharmacy-analytics
```

### 2. إعداد البيئة المحلية

```bash
# تثبيت المكتبات
pnpm install

# بدء خادم التطوير
pnpm dev

# تشغيل الاختبارات
pnpm test

# تشغيل linter
pnpm lint
```

### 3. إنشاء فرع جديد

```bash
# تحديث الفرع الرئيسي
git checkout main
git pull origin main

# إنشاء فرع جديد
git checkout -b feature/your-feature-name
```

### 4. معايير الكود

#### تسمية الفروع
- `feature/description` - لإضافة ميزات جديدة
- `fix/description` - لإصلاح الأخطاء
- `docs/description` - لتحديثات التوثيق
- `refactor/description` - لإعادة هيكلة الكود
- `test/description` - لإضافة اختبارات

#### معايير الكود
- استخدم TypeScript بدلاً من JavaScript
- اتبع معايير ESLint والـ Prettier
- أضف تعليقات للكود المعقد
- اختبر الكود قبل الرفع

```bash
# تنسيق الكود
pnpm format

# فحص الكود
pnpm lint

# إصلاح الأخطاء
pnpm lint:fix
```

#### رسائل الـ Commits
```
feat: إضافة ميزة جديدة
fix: إصلاح خطأ
docs: تحديث التوثيق
style: تنسيق الكود
refactor: إعادة هيكلة
test: إضافة اختبارات
chore: تحديثات عامة
```

مثال:
```
feat: إضافة صفحة تقرير جديدة للمنتجات

- إضافة صفحة ProductsReport.tsx
- إضافة رسوم بيانية للمنتجات
- إضافة جدول بيانات المنتجات
- إضافة اختبارات للصفحة الجديدة

Closes #123
```

### 5. اختبار التغييرات

```bash
# تشغيل جميع الاختبارات
pnpm test

# تشغيل اختبارات محددة
pnpm test:watch

# فحص التغطية
pnpm test:coverage
```

### 6. رفع التغييرات

```bash
# إضافة الملفات المعدلة
git add .

# إنشاء commit
git commit -m "feat: وصف التغيير"

# رفع الفرع
git push origin feature/your-feature-name
```

### 7. إنشاء Pull Request

1. اذهب إلى صفحة المستودع على GitHub
2. انقر على "New Pull Request"
3. اختر الفرع الخاص بك
4. أكمل نموذج PR:

```markdown
## الوصف
وصف مختصر للتغييرات

## النوع
- [ ] ميزة جديدة
- [ ] إصلاح خطأ
- [ ] تحديث التوثيق

## الاختبارات
- [ ] تم اختبار التغييرات محلياً
- [ ] لا توجد أخطاء linting
- [ ] تم إضافة اختبارات جديدة

## قائمة التحقق
- [ ] تم تحديث التوثيق
- [ ] تم إضافة تعليقات للكود
- [ ] تم اختبار جميع الحالات

## الروابط ذات الصلة
Closes #123
```

### 8. عملية المراجعة

- سيقوم أحد المشرفين بمراجعة PR الخاص بك
- قد يطلب تغييرات أو تحسينات
- بعد الموافقة، سيتم دمج التغييرات

## معايير الجودة

### الأداء
- لا تضف ملفات كبيرة بدون ضرورة
- استخدم التخزين المؤقت عند الحاجة
- تجنب العمليات الثقيلة في الـ render

### الأمان
- لا تشارك بيانات حساسة
- استخدم متغيرات البيئة للمفاتيح
- تحقق من المدخلات دائماً
- اتبع معايير OWASP

### التوثيق
- وثق الدوال والمكونات
- أضف أمثلة للاستخدام
- حدث ملف README عند الحاجة

## الأسئلة الشائعة

### كيف أقوم بـ fork المستودع؟
1. اذهب إلى صفحة المستودع
2. انقر على زر "Fork" في الزاوية العلوية اليمنى
3. اختر حسابك

### كيف أقوم بـ sync الفرع الخاص بي؟
```bash
git fetch upstream
git rebase upstream/main
```

### كيف أقوم بـ revert التغييرات؟
```bash
git reset --hard HEAD~1
```

## الدعم

إذا كان لديك أسئلة:
- اطرح سؤالاً في Issues
- اتصل بنا عبر البريد الإلكتروني
- انضم إلى مجتمعنا

## الترخيص

بالمساهمة، فإنك توافق على أن تكون مساهمتك تحت نفس الترخيص الخاص بالمشروع.

---

شكراً لمساهمتك! 🙏
