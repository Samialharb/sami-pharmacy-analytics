name: 🔄 Daily Sync Odoo to Supabase

on:
  # تشغيل يومي الساعة 7 صباحاً (UTC+3)
  schedule:
    - cron: '0 4 * * *'  # 7 AM UTC+3 = 4 AM UTC
  
  # تشغيل يدوي
  workflow_dispatch:

jobs:
  sync:
    runs-on: ubuntu-latest
    
    steps:
      - name: 📥 Clone Repository
        uses: actions/checkout@v4
      
      - name: 🔧 Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22'
      
      - name: 🚀 Run Sync
        run: node scripts/sync_odoo_daily.mjs
