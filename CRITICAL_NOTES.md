# ملاحظات حرجة عن المشروع

## 🔴 معلومات مهمة جداً - لا تنسى!

### عدد الطلبات الفعلي في Aumet ERP
- **pos.order (أوامر نقطة البيع): 28,016 طلب** ✅
- **sale.order (طلبات المبيعات): 62 طلب فقط** ✅ (تم التأكد من API)
- تم التحقق بتاريخ: 12 نوفمبر 2025
- المصدر: واجهة Aumet ERP الويب + API
- URL: https://health-path.erp-ksa.aumet.com

### Schema الصحيح في Supabase

#### 1. جدول `aumet_sales_orders` (10,000 سجل حالياً)
```sql
- id (uuid) - Primary Key
- aumet_id (int4) - معرف الطلب في Odoo ✅
- name (varchar) - رقم الطلب (مثل: Shop/17856) ✅
- partner_id (int4) - معرف العميل ✅
- amount_total (numeric) - المبلغ الإجمالي ✅
- state (varchar) - حالة الطلب (done, invoiced, etc.) ✅
```

#### 2. جدول `aumet_customers` (4,947 سجل)
```sql
- id (uuid) - Primary Key
- aumet_id (int4) - معرف العميل في Odoo ✅
- name (varchar) - اسم العميل ✅
- email (varchar) - البريد الإلكتروني ✅
- phone (varchar) - رقم الهاتف ✅
```

#### 3. جدول `aumet_products` (177 سجل)
```sql
- id (uuid) - Primary Key
- aumet_id (int4) - معرف المنتج في Odoo ✅
- name (varchar) - اسم المنتج ✅
- default_code (varchar) - الباركود ✅
- list_price (numeric) - السعر ✅
```

### المشاكل المكتشفة

#### 1. السكريبت يستخدم أسماء أعمدة خاطئة:
```python
# ❌ الكود الحالي في السكريبت:
{
    'order_id': order['id'],      # ❌ يجب أن يكون 'aumet_id'
    'customer_id': order.get('partner_id', [None])[0],  # ❌ يجب أن يكون 'partner_id'
    'location': order.get('warehouse_id', [None, 'Unknown'])[1],  # ❌ غير موجود في Schema
    ...
}
```

#### 2. عدد الطلبات الفعلي:
- ✅ `sale.order` في Odoo = **62 طلب فقط** (تم التأكد من API)
- ✅ `pos.order` في Odoo = **28,016 طلب** (طلبات نقطة البيع)
- ✅ `aumet_sales_orders` في Supabase = **10,000 سجل** (بيانات قديمة)

### الحل المطلوب

#### الخطوة 1: إصلاح أسماء الأعمدة في السكريبت
```python
# ✅ الكود الصحيح:
{
    'aumet_id': order['id'],  # ✅ استخدام aumet_id
    'name': order.get('name', ''),
    'partner_id': order.get('partner_id', [None])[0],  # ✅ استخدام partner_id مباشرة
    'amount_total': order.get('amount_total', 0),
    'state': order.get('state', ''),
    # ❌ حذف location لأنه غير موجود في Schema
}
```

#### الخطوة 2: تحديد نوع الطلبات المطلوبة
- **Option 1**: جلب `sale.order` (62 طلب) - طلبات مبيعات عادية
- **Option 2**: جلب `pos.order` (28,016 طلب) - طلبات نقطة البيع
- **السؤال**: أيهما تريد؟

#### الخطوة 3: تشغيل السكريبت مرة أخرى
- بعد إصلاح أسماء الأعمدة
- مع استخدام pagination الصحيح

### بيانات الاتصال
- Aumet URL: https://health-path.erp-ksa.aumet.com
- Database: health-path.erp-ksa.aumet.com
- Username: sami@aumet.com
- UID: 7
- Supabase URL: https://ajcbqdlpovpxbzltbjfl.supabase.co

### مشاكل أخرى معروفة
1. **صفحات تستخدم tRPC بدلاً من Supabase**:
   - Sales.tsx ❌
   - Inventory.tsx ❌
   - Products.tsx ❌
   - Customers.tsx ❌
   - Home.tsx ❌
   - Dashboard.tsx ✅ (تم التحديث)

### الخطوات التالية
1. ✅ تأكدنا من العدد الفعلي: 62 طلب (sale.order) أو 28,016 طلب (pos.order)
2. ✅ فهمنا Schema الصحيح في Supabase
3. 🔄 إصلاح أسماء الأعمدة في السكريبت
4. ⏳ تحديد نوع الطلبات المطلوبة (sale.order أم pos.order)
5. ⏳ اختبار السكريبت المعدل
6. ⏳ تحديث باقي الصفحات لاستخدام Supabase
